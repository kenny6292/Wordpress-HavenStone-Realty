"use client";import{useState}from"react";import{useRouter}from"next/navigation";type P={id:string;title:string;location:string;price_minor:number;property_type:string;listing_type:string;status:string;bedrooms:number;bathrooms:number;size_sqm:number|null;description:string;image_url:string|null;featured:boolean;agent_name:string|null;agent_phone:string|null};export default function PropertyForm({property}:{property:P}){const router=useRouter();const[s,setS]=useState("");async function save(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setS("Saving...");const f=new FormData(e.currentTarget),b=Object.fromEntries(f.entries());const r=await fetch("/api/admin/properties/"+property.id,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({...b,price:Number(b.price),bedrooms:Number(b.bedrooms),bathrooms:Number(b.bathrooms),size_sqm:b.size_sqm?Number(b.size_sqm):null,images:String(b.images||"").split("\n").map(x=>x.trim()).filter(Boolean),amenities:String(b.amenities||"").split("\n").map(x=>x.trim()).filter(Boolean),featured:b.featured==="on"})});if(r.ok){setS("Saved.");router.push("/admin/properties")}else setS((await r.json()).error||"Unable to save")}return <form className="form" style={{maxWidth:760,margin:"auto"}} onSubmit={save}><input name="title" defaultValue={property.title} placeholder="Property title" required/><input name="location" defaultValue={property.location} placeholder="Location" required/><input name="price" type="number" min="0" step="0.01" defaultValue={Number(property.price_minor)/100} placeholder="Price in NGN" required/><input name="property_type" defaultValue={property.property_type} placeholder="Property type"/><select name="listing_type" defaultValue={property.listing_type}><option value="sale">For Sale</option><option value="rent">For Rent</option></select><select name="status" defaultValue={property.status}><option value="available">Available</option><option value="pending">Pending</option><option value="sold">Sold</option><option value="rented">Rented</option></select><input name="bedrooms" type="number" min="0" defaultValue={property.bedrooms}/><input name="bathrooms" type="number" min="0" defaultValue={property.bathrooms}/><input name="size_sqm" type="number" min="0" defaultValue={property.size_sqm||""} placeholder="Size in sqm"/><input name="image_url" type="url" defaultValue={property.image_url||""} placeholder="Main image URL"/><textarea name="images" placeholder="Additional image URLs, one per line"/><textarea name="amenities" placeholder="Amenities, one per line"/><textarea name="description" defaultValue={property.description} placeholder="Description"/><input name="agent_name" defaultValue={property.agent_name||""} placeholder="Agent name"/><input name="agent_phone" defaultValue={property.agent_phone||""} placeholder="Agent phone"/><label><input name="featured" type="checkbox" defaultChecked={property.featured}/> Featured property</label><button className="btn">Save Changes</button>{s&&<p className="muted">{s}</p>}</form>}