"use client";import{useRouter}from"next/navigation";export default function Status({id,status}:{id:string;status:string}){const r=useRouter();return <select defaultValue={status} onChange={async e=>{const v=e.target.value;await fetch("/api/admin/viewings/"+id,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({status:v})});r.refresh()}}><option value="pending">Pending</option><option value="confirmed">Confirmed</option><option value="completed">Completed</option><option value="cancelled">Cancelled</option></select>}