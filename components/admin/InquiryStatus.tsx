"use client";import{useRouter}from"next/navigation";export default function Status({id,status}:{id:string;status:string}){const r=useRouter();return <select defaultValue={status} onChange={async e=>{const v=e.target.value;await fetch("/api/admin/inquiries/"+id,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({status:v})});r.refresh()}}><option value="new">New</option><option value="contacted">Contacted</option><option value="closed">Closed</option></select>}