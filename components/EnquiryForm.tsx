"use client";import{useState}from"react";
export default function EnquiryForm({propertyId}:{propertyId?:string}){const[s,setS]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setS("Sending...");const f=new FormData(e.currentTarget);const r=await fetch("/api/inquiries",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({property_id:propertyId||null,full_name:f.get("full_name"),email:f.get("email"),phone:f.get("phone"),message:f.get("message")})});setS(r.ok?"Enquiry received. Our team will contact you shortly.":"Unable to send enquiry.");if(r.ok)e.currentTarget.reset()}return <form className="form" onSubmit={submit}><input name="full_name" placeholder="Full name" required/><input name="email" type="email" placeholder="Email address"/><input name="phone" placeholder="Phone / WhatsApp" required/><textarea name="message" placeholder="How can we help?" required/><button className="btn" type="submit">Send Enquiry</button>{s&&<p className="muted">{s}</p>}</form>}