"use client";import{useState}from"react";
export default function ViewingForm({propertyId}:{propertyId?:string}){const[s,setS]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setS("Sending...");const f=new FormData(e.currentTarget);const r=await fetch("/api/viewings",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({property_id:propertyId||null,full_name:f.get("full_name"),email:f.get("email"),phone:f.get("phone"),preferred_date:f.get("preferred_date"),preferred_time:f.get("preferred_time"),message:f.get("message")})});setS(r.ok?"Viewing request received. We will confirm the appointment.":"Unable to submit request.");if(r.ok)e.currentTarget.reset()}return <form className="form" onSubmit={submit}><input name="full_name" placeholder="Full name" required/><input name="email" type="email" placeholder="Email address"/><input name="phone" placeholder="Phone / WhatsApp" required/><input name="preferred_date" type="date" required/><select name="preferred_time" defaultValue=""><option value="">Preferred time</option><option>Morning</option><option>Afternoon</option><option>Evening</option></select><textarea name="message" placeholder="Additional information"/><button className="btn" type="submit">Request Viewing</button>{s&&<p className="muted">{s}</p>}</form>}