import{redirect}from"next/navigation";import{requireAdmin}from"@/lib/auth";import{sql}from"@/lib/db";export default async function AdminProperties(){if(!(await requireAdmin()))redirect("/admin/login");const rows=await sql`select id,property_code,title,location,listing_type,status,price_minor from properties order by created_at desc`;return <main><section className="page-hero"><div className="container"><div className="eyebrow">Management</div><h1>Properties</h1><p><a className="btn" href="/admin/properties/new">Add Property</a></p><p className="muted">Live inventory from PostgreSQL.</p></div></section><section className="section"><div className="container"><div className="property-grid">{rows.map((p:any)=><article className="property-card" key={p.property_code}><div className="property-body"><span className="eyebrow">{p.property_code} · {p.status}</span><h3>{p.title}</h3><p>{p.location}</p><strong>₦{Number(p.price_minor/100).toLocaleString()}</strong><p><a className="btn secondary" href={"/admin/properties/"+p.id+"/edit"}>Edit</a></p><small>{p.listing_type}</small></div></article>)}</div></div></section></main>}