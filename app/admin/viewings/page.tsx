import{redirect}from"next/navigation";import{requireAdmin}from"@/lib/auth";import{sql}from"@/lib/db";import AdminNav from"@/components/admin/AdminNav";import ViewingStatus from"@/components/admin/ViewingStatus";export default async function AdminViewings(){if(!(await requireAdmin()))redirect("/admin/login");const rows=await sql`select v.id,v.full_name,v.phone,v.email,v.preferred_date,v.preferred_time,v.message,v.status,p.title from viewing_requests v left join properties p on p.id=v.property_id order by v.created_at desc`;return <main><section className="page-hero"><div className="container"><div className="eyebrow">Viewing management</div><h1>Viewing requests</h1><p className="muted">Live viewing requests from prospective clients.</p><AdminNav/></div></section><section className="section"><div className="container"><div className="admin-list">{rows.map((r:any)=><article className="admin-row" key={r.id}><strong>{r.full_name}</strong><span>{r.preferred_date}</span><span>{r.preferred_time||"Flexible"}</span><span>{r.phone}</span><ViewingStatus id={r.id} status={r.status}/><p>{r.title||"Property"} · {r.message||"No message"}</p></article>)}</div></div></section></main>}