import {sql} from "@/lib/db";
export default async function AdminInquiries(){const rows=await sql`select i.id,i.full_name,i.phone,i.email,i.message,i.status,i.created_at,p.title from property_inquiries i left join properties p on p.id=i.property_id order by i.created_at desc`;return <main><section className="page-hero"><div className="container"><div className="eyebrow">Lead management</div><h1>Enquiries</h1><p className="muted">Customer enquiries submitted through HavenStone.</p></div></section><section className="section"><div className="container"><div className="admin-list">{rows.map((r:any)=><article className="admin-row" key={r.id}><strong>{r.full_name}</strong><span>{r.phone}</span><span>{r.email||"No email"}</span><span>{r.title||"General enquiry"}</span><span>{r.status}</span><p>{r.message}</p></article>)}</div></div></section></main>}