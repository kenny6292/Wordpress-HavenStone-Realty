export const dynamic = "force-dynamic";

import Image from"next/image";import{sql}from"@/lib/db";import{notFound}from"next/navigation";import{EnquiryForm}from"@/components/EnquiryForm";export default async function Property({params}:{params:Promise<{id:string}>}){const{id}=await params;const rows=await sql`select * from properties where slug=${id} or lower(property_code)=lower(${id}) limit 1`;const p:any=rows[0];if(!p)notFound();return <main><section className="section"><div className="container"><span className="tag">{p.listing_type==="sale"?"For Sale":"For Rent"}</span><h1>{p.title}</h1><p className="muted">{p.location}</p>{p.image_url&&<Image src={p.image_url} alt={p.title} width={1600} height={1000} style={{width:"100%",height:"auto",borderRadius:16,marginTop:20}}/>}{Array.isArray(p.images)&&p.images.length>0&&<div className="grid" style={{marginTop:20}}>{p.images.map((src:string)=><Image key={src} src={src} alt={p.title} width={800} height={600} style={{width:"100%",height:"auto",borderRadius:12}}/>)}</div>}<div className="stats" style={{marginTop:24}}><div className="stat"><strong>{p.bedrooms}</strong><p>Bedrooms</p></div><div className="stat"><strong>{p.bathrooms}</strong><p>Bathrooms</p></div><div className="stat"><strong>{p.size_sqm||"—"}</strong><p>Sq m</p></div><div className="stat"><strong>₦{(Number(p.price_minor)/100).toLocaleString()}</strong><p>Price{p.listing_type==="rent"?" / year":""}</p></div></div><div className="split" style={{marginTop:60,alignItems:"start"}}><div><div className="eyebrow">Property overview</div><h2>Designed for the way you live.</h2><p className="muted">{p.description}</p>{Array.isArray(p.amenities)&&p.amenities.length>0&&<><h3>Key features</h3><ul className="muted">{p.amenities.map((x:string)=><li key={x}>{x}</li>)}</ul></>}</div><div className="card-body" style={{border:"1px solid #e4eaf2",borderRadius:14}}><h3>Interested in this property?</h3><p className="muted">Send an enquiry or request a private viewing.</p><a className="btn" href={"/viewing?property="+encodeURIComponent(p.id)}>Schedule a Viewing</a><EnquiryForm propertyId={p.id}/><a className="btn secondary" href="https://wa.me/2349013410129">WhatsApp an Agent</a></div></div></div></section></main>}