export const dynamic = "force-dynamic";

import Image from"next/image";import{sql}from"@/lib/db";import{notFound}from"next/navigation";export default async function Properties({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const q=await searchParams;const location=q.location||"";const type=q.type&&q.type!=="Any type"?q.type:"";const listing=q.status==="For Rent"?"rent":q.status==="For Sale"?"sale":"";const rows=await sql`select id,property_code,slug,title,location,price_minor,currency,listing_type,bedrooms,bathrooms,size_sqm,image_url from properties where status in ('available','pending') and (${location}='' or lower(location) like lower(${"%"+location+"%"})) and (${type}='' or lower(property_type)=lower(${type})) and (${listing}='' or listing_type=${listing}) order by featured desc,created_at desc`;return <main><section className="page-hero"><div className="container"><div className="eyebrow">HavenStone listings</div><h1>Find your next property.</h1><p className="muted">Browse live homes, apartments and investment opportunities.</p></div></section><section className="section"><div className="container"><form className="search"><div className="search-grid"><label>Location<input name="location" defaultValue={location} placeholder="Lekki, Ikoyi, Ajah..." /></label><label>Type<select name="type" defaultValue={type||"Any type"}><option>Any type</option><option>Apartment</option><option>House</option><option>Duplex</option><option>Land</option></select></label><label>Status<select name="status" defaultValue={q.status||"Buy or Rent"}><option>Buy or Rent</option><option>For Sale</option><option>For Rent</option></select></label><button className="btn">Search</button></div></form><div className="grid" style={{marginTop:40}}>{rows.map((p:any)=><article className="card" key={p.id}>{p.image_url&&<Image src={p.image_url} alt={p.title} width={1000} height={700}/>}<div className="card-body"><span className="tag">{p.listing_type==="sale"?"For Sale":"For Rent"}</span><h2>{p.title}</h2><p className="muted">{p.location}</p><p className="muted">{p.bedrooms} beds · {p.bathrooms} baths · {p.size_sqm||"—"} sqm</p><div className="price">₦{(Number(p.price_minor)/100).toLocaleString()}{p.listing_type==="rent"?" / year":""}</div><a className="btn" style={{marginTop:16}} href={"/properties/"+p.slug}>View Property</a></div></article>)}</div>{!rows.length&&<p className="muted" style={{marginTop:40}}>No matching properties found.</p>}</div></section></main>}