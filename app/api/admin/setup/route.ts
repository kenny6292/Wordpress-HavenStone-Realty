import{NextResponse}from"next/server";import{sql}from"@/lib/db";import{hashPassword}from"@/lib/auth";export async function POST(req:Request){if(req.headers.get("x-admin-bootstrap-secret")!==process.env.ADMIN_BOOTSTRAP_SECRET)return NextResponse.json({error:"Unauthorized"},{status:401});const{email,password}=await req.json();if(!email||!password||password.length<12)return NextResponse.json({error:"Email and password (12+ characters) required"},{status:400});await sql`insert into admin_users(email,password_hash) values(${email},${hashPassword(password)}) on conflict(email) do update set password_hash=excluded.password_hash`;return NextResponse.json({ok:true})}