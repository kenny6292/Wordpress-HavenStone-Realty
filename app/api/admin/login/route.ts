import{NextResponse}from"next/server";import{sql}from"@/lib/db";import{verifyPassword,createSession}from"@/lib/auth";export async function POST(req:Request){const{email,password}=await req.json();const rows=await sql`select id,password_hash from admin_users where lower(email)=lower(${email}) limit 1`;if(!rows[0]||!verifyPassword(password,rows[0].password_hash))return NextResponse.json({error:"Invalid credentials"},{status:401});await createSession(rows[0].id);return NextResponse.json({ok:true})}